// function wordLengths(arr) {
//    return arr => arr.map(x => x.length);
// }

const wordLengths = arr => arr.map(x => x.length);

const array1 = ["She", "sells", "seashells", "down", "by", "the", "seashore"];

// pass a function to map
const map1 = arr.map(x => x.length);

console.log(map1);