/*
Divison remainder
Complete the function divisionRemainder such that it returns the division remainder for the two numbers it receives: a & b.
*/
function divisionRemainder(a, b) {
    return a % b;
}

//sample usage
console.log(divisionRemainder(7, 2));
console.log(divisionRemainder(10, 2));