/*
Basic check
Implement a function canVote that returns true whenever the age is bigger than or equal to 18
*/
function canVote(age) {
    if (age >= 18) {
        return true;
    }
}

//sample usage
console.log(canVote(25));